<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.e_library">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logoelibrary"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logoelibrary"
        android:supportsRtl="true"
        android:theme="@style/Theme.ELibrary"
        android:usesCleartextTraffic="true">
        <activity android:name=".DetailBukuActivity"></activity>
        <activity android:name=".BukuKategoriActivity" />
        <activity android:name=".PdfViewActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>